#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        int R[3], G[3], B[3], r, g, b;
        for (int i = 0; i < 3; i++)
        {
            cin >> R[i];
        }
        for (int i = 0; i < 3; i++)
        {
            cin >> G[i];
        }
        for (int i = 0; i < 3; i++)
        {
            cin >> B[i];
        }
    }
    return 0;
}